apiVersion: v1
kind: Service
metadata:
  name: {{ include "mag7-chart.fullname" . }}
  labels:
    {{- include "mag7-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mag7-chart.selectorLabels" . | nindent 4 }}
